-- 최고관리자 역할의 표시 이름을 '시스템 관리자' 로 바꾼다.
--
-- 바뀌는 것은 화면에 보이는 이름뿐이다. 역할 코드(super_admin)는 그대로다.
-- 코드를 바꾸면 이 값을 문자열로 비교하는 곳(RLS 정책, 함수, 프런트)이 전부
-- 깨진다. 이름과 코드를 갈라 둔 이유가 이것이다.
update public.roles set name = '시스템 관리자' where code = 'super_admin';
